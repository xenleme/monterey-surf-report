import {
  renderSpots,
  renderWaterTemp,
  renderWindSpeed,
  renderSpotForecast
} from './render-elements';
import '../sass/styles.scss';

renderSpots();
renderWaterTemp();
renderWindSpeed();
renderSpotForecast(161);
renderSpotForecast(154);
renderSpotForecast(152);
